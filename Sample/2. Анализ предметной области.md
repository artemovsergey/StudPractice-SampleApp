

# Моделирование пользователей

Мы закончили созданием страницы-заглушки для регистрации
пользователей. Далее мы сделаем первый важный шаг – создадим модель
данных, описывающую пользователей нашего сайта, и реализуем механизм
хранения этих данных. Далее мы реализуем саму процедуру регистрации и
создадим страницу профиля пользователя. После этого разрешим им
осуществлять вход и выход, а потом узнаем, как защитить страницы от
несанкционированного доступа. Наконец, добавим сброс пароля. Конечная
цель следующих трех дней – создание страницы регистрации для нашего
сайта, но на данном этапе мы не готовы получать информацию о новых
пользователях, ее попросту негде хранить. Поэтому первый шаг к регистрации
пользователей – создание структуры данных для получения и хранения
информации о них.

## Создание структуры данных таблицы users

Создаем в HeidiSQL базу данных sample (если это возможно). Код на
языке SQL представлен ниже. Можно использовать этот код, а можно создать
базу и таблицу вручную. 

```sql
CREATE DATABASE `sample` /*!40100 COLLATE 'utf8_general_ci' */;
```

Далее, создаем таблицу users. Определяем аттрибуты: id (INT),
name(VARCHAR), email(VARCHAR). Аттрибуту id значение по умолчанию
выставляем AUTO_INCREMENT, а также выставляем значение id –
PRIMARY KEY. Код на языке SQL представлен ниже: 

```sql
USE `sample`;
CREATE TABLE `users` (
`id` INT NOT NULL AUTO_INCREMENT,
`name` VARCHAR(50) NULL,
`email` VARCHAR(50) NULL,
PRIMARY KEY (`id`)
)
COLLATE='utf8_general_ci'
;
```
Заполнение таблицы users пользователями. 

```sql
INSERT INTO `sample`.`users` (`name`, `email`) VALUES ('test1',
'test1@prep.scc');
INSERT INTO `sample`.`users` (`name`, `email`) VALUES ('test2',
'test2@prep.scc');
```

# Подключение к базе данных sample 

Создаем в проекте файл users.php. В файле header.php делаем ссылку
на эту страницу. Далее надо осуществить подключение к базе данных. Для
этого создайте в проекте папку includes, в которой создайте файл db.php, где
в параметрах укажите имя базы данных, логин и пароль. Ниже приводится код
db.php. Файл rb.php возьмите у преподавателя. 


