# Микросообщения пользователей

В процессе разработки ядра учебного приложения мы столкнулись с
четырьмя ресурсами – пользователи, сеансы, обновление, удаление, но
только первый из них связан с таблицей в базе данных. Теперь пришло время
добавить второй такой ресурс: микросообщения пользователей – короткие
сообщения, связанные с конкретными пользователями
Создадим модель данных Micropost, свяжем ее с моделью User. Затем
напишем формы, необходимые для обработки и отображения- результатов.
Завершим создание нашего крохотного клона Твиттера, добавив понятие
следования за пользователями для получения потока их микросообщений. 

# Задание
Создайте в базе данных таблицу microposts, атрибуты которой будут: id
(INT), content (TEXT), а также поле user_id, которое будет связующим звеном
с таблицей users. Сделайте таблицу microposts дочерней таблицей users.
Далее, создайте microposts.php


# Вывод микросообщений

У нас пока не реализована возможность создания микросообщений с
помощью веб-браузера – она появится позже, но это не мешает нам
реализовать их отображение (и ее тестирование). Следуя по стопам Twitter,
мы будем отображать микросообщения не на отдельной странице с их
списком, а непосредственно на странице профиля пользователя. Начнем с
довольно простых шаблонов добавления микросообщений в профиль
пользователя. 

# Добавление микросообщений на страницу профиля пользователя

```html
<div class="row">
 <aside class="col-md-4">
 <section class="user_info">
 <h1>
 <?php echo $_SESSION['logged_user']['name'] ?>
 </h1>
 </section>
 </aside>
<?php
$user = R::findOne('users','email=?',array($_SESSION['logged_user']['email']));
$usermicroposts = $user->with('order by id ')->ownMicropostsList;
?>
 <div class="col-md-8">
 <h3> <?php echo '<h3>Cообщения('.$user-
>countOwn('microposts').')</h3><hr>'; ?> </h3>
 <ol class="microposts">
 <?php foreach($usermicroposts as $i){ ?>
 <?php echo $i['content']?>

 <br>
 <br>
 <br>

 <?php } ?>
 </ol>
 </div>
 </div>

```

Стили css для микросообщений. 

```css
.microposts {
list-style: none;
padding: 0;
li {
padding: 10px 0;
border-top: 1px solid #e8e8e8;
}
.user {
margin-top: 5em;
padding-top: 0;
}
.content {
display: block;
margin-left: 60px;
img {
display: block;
padding: 5px 0;
}
}
.timestamp {
color: $gray-light;
display: block;
margin-left: 60px;
}
.gravatar {
float: left;
margin-right: 10px;
margin-top: 5px;
}
}
aside {
textarea {
height: 100px;
margin-bottom: 5px;
}
}
span.picture {
margin-top: 10px;
input {
border: 0;
}
}

```

# Манипулирование микросообщениями

Мы закончили работу над моделью данных и шаблонами отображения
микросообщений, теперь обратим наше внимание на веб-интерфейс для их
создания. В этом разделе мы также увидим первый намек на поток (ленту)
сообщений. Наконец, так же как с пользователями, мы сделаем возможным
удаление микросообщений через веб-интерфейс. 


Создание микросообщений
Добавление формы для создания микросообщений на главной странице
(index.php). При этом форма должна появляться, когда пользователь
авторизирован. 

Задание: реализуйте отображение формы при заданном условии
Используйте для этого условие 

```php
<?php if($_SESSION['logged_user']['name']){ ?>
 …
 <? }
 else {
 ?>
 …
 <? } ?>
```

Далее, если пользователь залогинился покажите ему форму c левой стороны 

```html
<div class="row">
 <aside class="col-md-4">
 <section class="micropost_form">
 <form action="index.php" method="POST" class="form-signin" role="form">
 <textarea rows="4" cols="50" type="text" name="textarea" required autofocus></textarea>
 <button class="btn btn-lg btn-primary btn-block" type="submit" name="submit">Post</button>
 </form>
 </section>
 </aside>
 </div>
```
Процесс генерации нового микросообщения 

# Прото-лента

Добавление только сообщений пользователя на главную страницу 

```html
<div class="row">
 <aside class="col-md-4">
 <section class="micropost_form">
 <form action="index.php" method="POST" class="form-signin" role="form">
 <textarea rows="4" cols="50" type="text" name="textarea" required
autofocus></textarea>
 <button class="btn btn-lg btn-primary btn-block" type="submit"
name="submit">Post</button>
 </form>
 </section>
 </aside>
 <?php
 $user =
R::findOne('users','email=?',array($_SESSION['logged_user']['email']));
 $usermicroposts = $user->with('order by id desc')->ownMicropostsList;
 ?>
 <div class="col-md-8">
 <h3> <?php echo '<h3>Cообщения('.$user->countOwn('microposts').')</h3><hr>'; ?>
</h3>
 <ol class="microposts">
 <?php foreach($usermicroposts as $i){ ?>
 <a href="profile.php?id=<?=$i['id']?>"><?php echo
$_SESSION['logged_user']['name'] ?></a>
 <br>
 <?php echo $i['content']?>
 <br>
 <a href="delete.php?id=<?=$i['id']?>" class="btn btn-info">Удалить</a>

 <br>
 <br>
 <br>
 <?php } ?>
 </ol>
 </div>
 </div>
 <?php
 if( isset($_POST['submit'])) // проверка нажатия кнопки
 {
 //проверка на уникальность пользователей в базе users
 $user=R::findOne('users','name=?',array($_SESSION['logged_user']['name'])); // определяем текущего user
 $micropost=R::dispense('microposts'); // определяем micropost
 $micropost->content=$_POST['textarea']; // редактируем micropost
 $user->ownMicropostsList[]=$micropost; // связываем micropost

 R::store($user); // сохраняем

 // очищаем массив $_POST обновлением
 echo 
```

# Удаление микросообщений

Последнее, что осталось добавить в ресурс Microposts, – возможность
удаления сообщений. Так же как в случае с удалением учетных записей мы
реализуем это добавлением ссылок «delete» (удалить). Но, в отличие от
учетных записей, удаление которых было доступно только администраторам,
микросообщения могут удаляться лишь их авторами. Ниже представлен код
нового файла delete.php.


# Изображения в микросообщениях

Теперь, когда у нас реализованы все необходимые операции с
микросообщениями, в этом разделе мы реализуем добавление изображений в
микросообщения. Начнем с простой версии, достаточной для окружения
разработки, а затем дополним ее рядом усовершенствований, чтобы сделать
возможной выгрузку изображений в эксплуатационном окружении. Это
нововведение содержит два основных видимых элемента: поле формы для
загрузки изображений и собственно изображения в микросообщениях. 


**Задание**. Добавить изображений в форму создания микросообщения

```html
<form enctype="multipart/form-data" action="" name="form" method="post">

 <div class="file-upload">
 <label>
 <input type="file" name="file">
 <span>Выбрать файл</span>
 </label>
 </div>

 <div class="container">
 <input type="text" id="filename" class="filename" disabled>
 </div>

 </form>
```
Или 

```html
<form enctype="multipart/form-data" action="home.php" method="POST"
role="form">
 <textarea rows="4" cols="50" type="text" name="textarea" required
autofocus></textarea>

 <button class="btn btn-lg btn-primary btn-block" id="submit"
type="submit" name="submit">Post</button>

 <br>
 <div class="file-upload">
 <label>
 <input type="file" name="file">
 <span>Выбрать файл</span>
 </label>
 </div>

 <!-- <div class="container">
 <input type="text" id="filename" class="filename" disabled>
 </div> -->

 </form>
```

**Задание**. отображать кнопку удалить, если сообщения текущего
пользователя


